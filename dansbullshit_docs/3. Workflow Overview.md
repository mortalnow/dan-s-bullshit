# Workflow Overview

## Core Workflows

### Workflow 1: User Registration & Approval

A new user must register, wait for admin approval, then log in to access the submission form.

```mermaid
sequenceDiagram
    participant User
    participant Browser
    participant FastAPI
    participant DB

    User->>Browser: Visit /register
    Browser->>FastAPI: GET /register
    FastAPI-->>Browser: register.html

    User->>Browser: Fill form, submit
    Browser->>FastAPI: POST /register (email, password, name)
    FastAPI->>DB: create_user(status=PENDING, is_admin=False)
    DB-->>FastAPI: User created
    FastAPI-->>Browser: register_success.html

    Note over User,DB: User waits for admin approval

    rect rgb(255, 229, 0)
        Note over FastAPI,DB: Admin Flow
        FastAPI->>DB: list_users()
        DB-->>FastAPI: [pending users]
        FastAPI->>DB: update_user_status(email, APPROVED)
    end

    User->>Browser: Visit /admin/login
    Browser->>FastAPI: POST /admin/login (email, password)
    FastAPI->>DB: get_user_by_email(email)
    DB-->>FastAPI: User (status=APPROVED)
    FastAPI-->>Browser: Set cookie, redirect to /admin
```

**Steps:**
1. User fills out registration form (name, email, password)
2. Server creates user record with `status=PENDING`
3. Admin logs in, navigates to user moderation (`/admin?mode=users`)
4. Admin clicks "Approve" on the pending user
5. User can now log in and access the submission form

### Workflow 2: Quote Submission & Moderation

An approved user submits a quote, which enters the pending queue for admin review.

```mermaid
sequenceDiagram
    participant User
    participant Browser
    participant FastAPI
    participant DB

    User->>Browser: Visit /submit
    Browser->>FastAPI: GET /submit
    FastAPI->>FastAPI: Verify cookie (must be APPROVED user)
    FastAPI-->>Browser: submit.html

    User->>Browser: Write quote, submit
    Browser->>FastAPI: POST /submit (content, submitted_by)
    FastAPI->>DB: content_hash(content)
    FastAPI->>DB: create_quote(status=PENDING)
    DB-->>FastAPI: QuoteResponse
    FastAPI-->>Browser: submit_success.html

    rect rgb(255, 229, 0)
        Note over FastAPI,DB: Admin Moderation
        FastAPI->>DB: list_quotes(status=PENDING)
        DB-->>FastAPI: [pending quotes]

        alt Approve
            FastAPI->>DB: update_quote(status=APPROVED, verified_by=admin)
        else Reject
            FastAPI->>DB: update_quote(status=REJECTED, verified_by=admin)
        else Edit + Approve
            FastAPI->>DB: update_quote(content=edited, status=APPROVED)
        end
    end
```

**Steps:**
1. Authenticated user visits `/submit` (redirects to login if no cookie)
2. User fills in quote content and submitter name
3. Server computes SHA-256 hash, checks for duplicates, inserts with `status=PENDING`
4. Admin sees quote in moderation queue (`/admin?mode=moderation`)
5. Admin can: approve, reject, edit content, edit submitter, or use bulk approve/reject all

### Workflow 3: Quote Dispensing (Homepage)

The homepage shows one random approved quote with shuffle and like functionality, all via AJAX.

```mermaid
sequenceDiagram
    participant Visitor
    participant Browser
    participant FastAPI
    participant DB

    Visitor->>Browser: Visit /
    Browser->>FastAPI: GET /
    FastAPI->>DB: random_approved()
    DB-->>FastAPI: QuoteResponse (or null)
    FastAPI-->>Browser: index.html with quote

    loop Shuffle clicks
        Visitor->>Browser: Click "Shuffle"
        Browser->>FastAPI: GET /api/quotes/random
        FastAPI->>DB: random_approved()
        DB-->>FastAPI: QuoteResponse
        FastAPI-->>Browser: JSON
        Browser->>Browser: Update DOM (no reload)
    end

    Visitor->>Browser: Click "Oh, Shit!"
    Browser->>FastAPI: POST /api/quotes/{id}/like
    FastAPI->>DB: increment_likes(id)
    DB-->>FastAPI: QuoteResponse (updated likes)
    FastAPI-->>Browser: JSON
    Browser->>Browser: Update like count
```

**Key details:**
- Initial page load is server-rendered (SEO-friendly, works without JS)
- Subsequent shuffles use `fetch()` to the JSON API -- no page reload
- Like button is unauthenticated -- anyone can click it (no rate limiting)
- Font size dynamically scales to fit the quote container (`fitQuoteToContainer()`)

### Workflow 4: Rejected Quote Resubmission

Regular users can see their own submission history and resubmit rejected quotes.

```mermaid
sequenceDiagram
    participant User
    participant Browser
    participant FastAPI
    participant DB

    User->>Browser: Visit /admin (as regular user)
    Browser->>FastAPI: GET /admin
    FastAPI->>FastAPI: Verify cookie, detect non-admin
    FastAPI->>DB: list_quotes(submitted_by=user_email)
    DB-->>FastAPI: [user's quotes with statuses]
    FastAPI-->>Browser: admin.html (archive mode, own quotes)

    User->>Browser: Edit rejected quote, click "Resubmit"
    Browser->>FastAPI: POST /admin/resubmit/{id} (edited content)
    FastAPI->>DB: Verify ownership + REJECTED status
    FastAPI->>DB: update_quote(status=PENDING, content=edited)
    FastAPI-->>Browser: Redirect to /admin
```

## Data Flow

```mermaid
flowchart LR
    subgraph Input
        WebForm["Web Form<br/>(POST /submit)"]
        API["JSON API<br/>(POST /api/quotes)"]
    end

    subgraph Processing
        Hash["SHA-256<br/>Dedup Check"]
        Insert["Insert as<br/>PENDING"]
    end

    subgraph Moderation
        Queue["Admin Queue"]
        Decision{Approve?}
    end

    subgraph Output
        Public["Public Homepage<br/>/ & /api/quotes"]
        Rejected["User Dashboard<br/>(Resubmit)"]
    end

    WebForm --> Hash
    API --> Hash
    Hash -->|unique| Insert
    Hash -->|duplicate| Return["Return existing"]
    Insert --> Queue
    Queue --> Decision
    Decision -->|Yes| Public
    Decision -->|No| Rejected
    Rejected -->|Resubmit| Hash
```

## Authentication Flow

```mermaid
flowchart TD
    Request["Incoming Request"]
    Cookie{"Has admin_token<br/>cookie?"}
    Parse["Parse cookie:<br/>email:password:role"]
    EnvCheck{"Matches .env<br/>admin creds?"}
    DBCheck{"Matches DB<br/>user record?"}
    RoleCheck{"role == admin?"}
    StatusCheck{"status ==<br/>APPROVED?"}
    AdminCtx["AdminContext<br/>(is_admin=true)"]
    UserCtx["AdminContext<br/>(is_admin=false)"]
    Reject401["401 Unauthorized"]
    Reject403["403 Forbidden"]

    Request --> Cookie
    Cookie -->|No| Reject401
    Cookie -->|Yes| Parse
    Parse --> EnvCheck
    EnvCheck -->|Yes| AdminCtx
    EnvCheck -->|No| DBCheck
    DBCheck -->|No match| Reject401
    DBCheck -->|Match| RoleCheck
    RoleCheck -->|Admin| AdminCtx
    RoleCheck -->|User| StatusCheck
    StatusCheck -->|APPROVED| UserCtx
    StatusCheck -->|PENDING| Reject403
```

**Notes:**
- `.env` admins always take priority (checked first)
- Database users are checked second
- The `AdminContext` object is used everywhere despite the name -- it represents any authenticated user
- `get_current_admin()` requires `is_admin=True`; `get_current_user()` allows both roles but requires `status=APPROVED`

## State Management

The application is stateless from the server's perspective:

- **No server-side sessions** -- all auth state lives in the cookie
- **No in-memory cache** -- every request hits the database
- **Settings cached once** -- `get_settings()` is `@lru_cache(maxsize=1)`, parsed from `.env` at first call
- **Database connection** -- stored on `app.state.db_client`, created during lifespan startup

## Error Handling

| Layer | Strategy |
|---|---|
| **HTTP 404/500** | Global exception handlers redirect to `/error` page |
| **Database errors** | `MongoDBError` / `LocalDBError` caught in every route, converted to HTTP 502 |
| **Auth failures** | `HTTPException(401)` or `HTTPException(403)` raised, web routes redirect to login |
| **Duplicate content** | Silently returns existing quote (no error) |
| **Validation** | Pydantic handles request validation; FastAPI returns 422 automatically |
| **Local vs Prod error detail** | Local mode shows full error message; production says "Internal database error" |
